// index.tsx
// type Props = {resetHandler: () => void; errorMsg: string};

// export const UploadErr: React.FC<Props> = ({
//     resetHandler,
//     errorMsg,
// }) => {
//     resetHandler();
//     const errorDisp = returnErrDisp(errorMsg);
// }

// // utils.tsx
// export const returnErrDisp = (errorMsg: string) => {
//     return errorMsg.split(/\n/).map((item) => ({
//         return <li key={index}>{item}</li>;
//     }));
// }
// export const returnErrDisp = (errorMsg: string) => {
//     return errorMsg.split(/\n/).map((item) => ({
//         type: xxx,
//         lineNo: xxxx,
//         error: xxxx,
//     }));
// }